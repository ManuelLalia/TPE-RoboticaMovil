function path = a_star(map, pose)
%A_STAR Summary of this function goes here
%   Detailed explanation goes here

start = [pose(1), pose(2)];
start = world2grid(map_, start);

obj = [12.5, 15];
obj = world2grid(map_copy, obj);

planner = plannerAStarGrid(map_copy);
path = plan(planner, start, obj);

show(planner)
% hold on
% plot(pathObj.States(:,1), pathObj.States(:,2), 'r','LineWidth',2)
end

